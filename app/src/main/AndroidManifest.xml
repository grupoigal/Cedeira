<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.grupoigal.anaruth.cedeira">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainGaleria"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainConcello"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainQR"
            android:label="@string/title_activity_main_qr"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainParticipa"
            android:label="@string/title_activity_main_participa"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainTurismo"
            android:label="@string/title_activity_main_turismo"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainGastroniomia"
            android:label="@string/title_activity_main_gastroniomia"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainWeb"
            android:label="@string/title_activity_main_web"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ListaTlf"
            android:label="@string/title_activity_lista_tlf"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Turismo"
            android:label="@string/title_activity_turismo"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ListaUnaFicha"
            android:label="@string/title_activity_lista_una_ficha"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Noticias"
            android:label="@string/title_activity_noticias"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Noticia"
            android:label="@string/title_activity_noticia"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Eventos"
            android:label="@string/title_activity_eventos"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Evento"
            android:label="@string/title_activity_evento"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Actualidade"
            android:label="@string/title_activity_actualidade"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".Hosteleria"
            android:label="@string/title_activity_hosteleria"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Rutas_San_Andres"
            android:label="@string/title_activity_rutas__san__andres"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CargaWeb"
            android:label="@string/title_activity_carga_web"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>